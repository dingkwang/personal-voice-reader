[project]
name = "voice-reader-indextts"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = ["modal==1.5.5", "fastapi==0.141.1", "psycopg[binary]==3.3.5"]

[dependency-groups]
dev = ["pytest==9.1.1", "httpx==0.28.1"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
